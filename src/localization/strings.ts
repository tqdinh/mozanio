import LocalizedStrings from 'react-native-localization';
import english from './strings/en';
import korea from './strings/ko';

export const strings = new LocalizedStrings({
  en: english,
  // ko: korea,
});
